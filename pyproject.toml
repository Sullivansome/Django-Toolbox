[project]
name = "toolbrighteng"
version = "0.1.0"
description = "Django API and server for tool.brighteng.org"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.4",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
